# For local runs in user space.

version: '3'

services:
  app:
    image: ${IMAGE}
    volumes:
      - ../..:/app
      - ~/.aws:/root/.aws
      # TODO(gp): No need to mount the filesystem.
      #- ../docker_build/fstab:/etc/fstab
    privileged: true
    cap_add:
      - SYS_ADMIN
