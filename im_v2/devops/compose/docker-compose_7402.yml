version: '3.5'

services:
  # Docker container running Postgres DB.
  im_postgres7402:
    image: postgres:13
    restart: "no"
    environment:
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - im_postgres7402_data:/var/lib/postgresql/data
volumes:
  im_postgres7402_data: {}

networks:
  default:
    #name: im_postgres7402_network
    name: main_network