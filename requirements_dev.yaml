channels:
  #- quantopian
  - conda-forge
dependencies:
  # ############################################################################
  # python dev
  # ############################################################################
  #- certifi
  #- pytables
  #- six
  #- xlrd
  #- xmltodict
  - pytest
  - pytest-cov
  - pytest-instafail
  - pytest-xdist
  - python >= 3.7
  - python-dateutil
  - pytz
  - tqdm
  # ############################################################################
  # pydata
  # ############################################################################
  - matplotlib
  - networkx
  - numba
  - numpy <= 1.17
  - pandas
  - pandas-datareader=0.8.0     # PartTask344.
  - pyarrow
  - pywavelets
  - scikit-learn
  - scipy
  - seaborn
  - statsmodels
  # ############################################################################
  # jupyter
  # ############################################################################
  - ipykernel
  - jupyter
  - jupyter_contrib_nbextensions
  - jupyter_nbextensions_configurator
  - jupytext
  - nb_conda
  - nb_conda_kernels
  # ############################################################################
  # Linter
  # ############################################################################
  #- pyment                   # conda install -c akode pyment
  - autoflake
  - black
  - flake8
  - isort
  - mypy
  - pydocstyle
  - pylint
  - yapf                     # Superseded by black, but useful for notebooks.
  # ############################################################################
  # Latex
  # ############################################################################
  - graphviz                # Also for dataflow graph visualization.
  - pandoc
  - pandocfilters
  # ############################################################################
  # AWS
  # ############################################################################
  - awscli
  - boto3
  - s3fs
  # ############################################################################
  # pymc3
  # ############################################################################
  #- pydot
  #- python-graphviz        # To plot pymc3 graphical models.
  - arviz                   # Needed by pymc3 for some plotting functionality.
  - mkl-service             # pymc3 expects it; not sure why conda doesn't solve for it.
  - pymc3
  # ############################################################################
  # Misc
  # ############################################################################
  #- csvkit                  # To process csv.
  #- gdrive                  # To interact with Google Drive.
  #- jq                      # To process json.
  #- mxnet                   # Required by gluon (e.g., gluon-ts, gluon-nlp).
  #- pymongo
  #- rclone                  # To back up Google Drive (conda install -c hcc rclone)
  #- ruby
  - beautifulsoup4
  - git                     # To use latest git client.
  - joblib
  - pygraphviz              # For dataflow graph visualization.
  - pymongo
  - scrapy
  # ############################################################################
  # Pip
  # ############################################################################
  - pip
  - pip:
    #- pyannotate           # Enable this to automatically infer types.
    #- ta                   # Technical analysis package.
    #- trading-calendars
    # ##########################################################################
    # Keep the following section unchanged in order, since we need to install
    # mxnet first.
    - mxnet >= 1.6.0
    - gluonnlp              # gluon* and mxnet need to be installed through pip
    - gluonts               # since conda doesn't support / has old packages.
